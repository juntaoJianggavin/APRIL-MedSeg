# Evaluate CVC-ClinicDB model trained with 80/20 split (same random_state as training).
# Evaluate CVC-ClinicDB model trained with 80/20 split (same random_state as training). (中文翻译待补充)
# Usage:
# Usage: (中文翻译待补充)
#   python test.py --config configs/intro_to_datasets/cvc_clinicdb_eval.yaml \
# python test.py --config configs/intro_to_datasets/cvc_clinicdb_eval.yaml \ (中文翻译待补充)
#       --checkpoint output/cvc_clinicdb/best_model.pth --save_pred
# --checkpoint output/cvc_clinicdb/best_model.pth --save_pred (中文翻译待补充)

model:
  num_classes: 2
  img_size: 352
  encoder:
    name: timm_resnet50
    pretrained: false
    in_channels: 3
    params: {}
  decoder:
    name: unet
    params: {}
  bottleneck:
    name: none

data:
  type: generic
  img_size: 352
  root_dir: ./data/CVC-ClinicDB
  val_dir: ./data/CVC-ClinicDB
  img_suffix: .png
  mask_suffix: .png
  train_ratio: 0.8
  val_ratio: 0.2
  random_state: 42

training:
  batch_size: 8
evaluation:
  include_background: false  # set true to include background class in metrics
